// Ejemplo 1: for Each para imprimir elementos de una lista, en el orden en que se encuentra en la lista
const numbers = [1, 6, 3, 7, 5]
console.log("Ejemplo 1: Imprimiendo los elementos de una lista")
numbers.forEach(num => console.log(num))
